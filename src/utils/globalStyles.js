import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#eee',
    alignItems: 'center'
  },

  verticalCenter: {
    justifyContent: 'center'
  }

});