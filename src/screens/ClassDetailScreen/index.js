import { Text, View } from 'react-native';
import globalStyles from '../../utils/globalStyles';

export default function ClassDetailScreen() {
  return (
    <View style={globalStyles.container}>
      <Text>Detalhes da turma</Text>
    </View>
  )
}